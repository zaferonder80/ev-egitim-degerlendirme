import { AppShell, formatDate } from "@/components/AppShell";
import { Card, CardContent } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { History } from "lucide-react";

export default function AdminAuditLogs() { const query = trpc.admin.auditLogs.useQuery(); return <AppShell role="ADMIN"><div><p className="text-sm font-medium text-teal-700">İzlenebilirlik</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-[#0b385d]">Denetim kayıtları</h1><p className="mt-2 text-sm text-slate-500">Yönetimsel işlemler ve kritik değişikliklerin kaydı.</p></div><Card className="mt-7 border-slate-200 shadow-sm"><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-6 py-4">Tarih</th><th className="px-4 py-4">Eylem</th><th className="px-4 py-4">Varlık</th><th className="px-4 py-4">Kimlik</th><th className="px-6 py-4">Detay</th></tr></thead><tbody className="divide-y divide-slate-100">{query.data?.length ? query.data.map(log => <tr key={log.id}><td className="px-6 py-4 text-slate-600">{formatDate(log.createdAt)}</td><td className="px-4 py-4"><span className="font-medium text-[#0b385d]">{log.action}</span></td><td className="px-4 py-4 text-slate-600">{log.entityType}</td><td className="px-4 py-4 text-slate-600">{log.entityId ?? "—"}</td><td className="px-6 py-4 text-xs text-slate-500">{log.newValue ? JSON.stringify(log.newValue) : "—"}</td></tr>) : <tr><td colSpan={5} className="px-6 py-16 text-center"><History className="mx-auto h-8 w-8 text-slate-300" /><p className="mt-3 text-slate-500">Henüz denetim kaydı bulunmuyor.</p></td></tr>}</tbody></table></div></CardContent></Card></AppShell>; }
